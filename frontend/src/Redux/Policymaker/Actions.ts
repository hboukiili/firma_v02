import { policyMakerSlice } from "./Slices";

export const {
    SetEndDate,
    SetStartDate,
    SetWaterShedId,
    SetBand,
    SetIsSubmit,
    SetSurfaceVariable,
    SetWeather,
    SetisLoading,
    SetChartData,
    SetDates,
    SetSubWaterShedId,
    SetIsBaseMap,
    SetFlux,
    SetIsGeoRaster,
    SetLoadingMsg,
} = policyMakerSlice.actions